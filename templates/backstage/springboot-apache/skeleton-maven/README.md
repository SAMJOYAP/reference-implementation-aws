# ${{ values.projectName }}

Generated by Backstage template: **Java Web App (Apache + Spring Boot + Maven)**.

## Configuration
- Java: `${{ values.javaVersion }}`
- Spring Boot: `${{ values.springBootVersion }}`
- Build Tool: `${{ values.buildTool }}`
- Packaging: `${{ values.packaging }}`
- Spring Web: `${{ values.useSpringWeb }}`
- Actuator: `${{ values.useActuator }}`
- Lombok: `${{ values.useLombok }}`
- Ingress Host (if enabled): `${{ values.hostPrefix }}.${{ values.baseDomain }}`

## Build
```bash
mvn clean verify
```

## Run (local)
```bash
mvn spring-boot:run
```

## Kubernetes
Deployment manifests are managed in the GitOps repository.

## CI/CD
- Pipeline: `.github/workflows/ci.yaml` (`DevSecOps Pipeline`)
- Includes: SonarQube SAST/SCA, Trivy image scan, SBOM, Cosign sign/verify, ECR push
- Required secrets: `AWS_ROLE_ARN`, `SONAR_TOKEN`, `SONAR_HOST_URL`

